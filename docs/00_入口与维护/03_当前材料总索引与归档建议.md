# 当前材料总索引与归档建议

更新时间：2026-04-20  
整理范围：`D:\newstart` 仓库内当前可见的文档、提交材料、测试证据、截图、日志、压缩包、模型源资产与工具输出  
整理原则：已完成第二轮物理整理；今后不再把非源码材料堆回仓库根目录，所有新增材料必须直接进入对应层级

## 1. 这份文档解决什么问题

这份文档用于回答四个问题：

1. 当前仓库里到底有哪些非源码材料层。
2. 哪些目录是正式材料，哪些是内部事实层，哪些只是调试归档。
3. 当同一主题在多个目录里重复出现时，应以哪一层为准。
4. 后续新增材料应该放到哪里，避免再次做“根目录大扫除”。

## 2. 当前材料全景结论

当前仓库内的非源码材料已经收口为 8 个主要层级：

1. 工程事实与内部知识层：`docs/`
2. AI 投喂与写作辅助层：`docs/30_AI写作与投喂/长庚环_AI投喂资料包/`
3. 测试证据层：`test-evidence/`
4. 主交付层：`最终材料/`
5. 评审解释层：`附件_评委增强理解包_完整版/`
6. 历史保留层：`材料提交版-不可以删除/` 与 `docs/90_历史交付与模板/`
7. 调试归档层：`artifacts/`
8. 工具与源资产辅助层：`output/`、`tools/avatar_source_assets/`

当前的核心变化不是“写了一份索引”，而是已经完成实际落位：

- 根目录散落截图已迁入 `artifacts/root_archive/ui_captures/`
- 根目录日志、XML dump、shared prefs、bootstrap 状态等已迁入 `artifacts/root_archive/debug_logs_and_state/`
- 崩溃日志已迁入 `artifacts/root_archive/crash_dumps/`
- 本地数据库快照与原 `debug_export/` 已迁入 `artifacts/root_archive/db_exports/`
- Playwright 根级输出已迁入 `artifacts/tool_output/playwright/`
- 根目录 APK、ZIP、README PDF 和协议附件已迁入 `最终材料/` 的明确子目录
- 根目录 3D 源资产已迁入 `tools/avatar_source_assets/`

结论：

- 仓库根目录不应再承载分发文件、截图、日志、数据库快照或模型源资产。
- 根目录现在只应保留源码目录、构建配置、密钥配置和项目入口文件。
- 后续非源码整理应以“维护规则”代替“再次清场”。

## 3. 当前材料目录总表

下表按当前实际目录统计整理。

| 路径 | 文件数 | 体积 | 材料类型 | 当前角色 | 建议定位 |
| --- | ---: | ---: | --- | --- | --- |
| `docs/` | 134 | 101.63 MB | 内部文档、图示、模板、写作材料 | 主知识库 | 主事实与主索引层 |
| `test-evidence/` | 1102 | 40.95 MB | 测试记录、截图、日志、汇总稿 | 正式测试证据库 | 正式证据层 |
| `最终材料/` | 21 | 3732.28 MB | 最终导出文档、视频、APK、ZIP、协议附件、快照 | 当前主交付包 | 对外交付层 |
| `附件_评委增强理解包_完整版/` | 46 | 17.93 MB | 评委阅读路线与解释性材料 | 评审辅助包 | 对外交付补充层 |
| `材料提交版-不可以删除/` | 10 | 49.42 MB | 历史提交稿、指南、旧版材料 | 历史保留包 | 历史归档层 |
| `artifacts/` | 630 | 314.88 MB | 调试截图、验证日志、过程留档、工具输出归档 | 开发过程证据库 | 调试归档层 |
| `output/` | 1 | 0.00 MB | 工具临时输出占位说明 | 过渡缓冲区 | 默认保持空目录 |
| `tools/avatar_source_assets/` | 4 | 117.32 MB | 3D 角色源资产与动作源模型 | 资产源文件层 | 工具辅助层 |

说明：

- `output/` 当前只保留目录说明文件，稳定保留的工具输出应进入 `artifacts/tool_output/`
- 原根目录 `debug_export/` 已不再单独存在，已并入 `artifacts/root_archive/db_exports/debug_export/`

## 4. 各材料层的真实职责

### 4.1 `docs/`：当前正式知识库

`docs/` 已完成结构化整理，顶层只保留总入口 `README.md` 和主题目录。

当前分层为：

- `00_入口与维护/`
- `10_项目事实与架构/`
- `20_专题系统说明/`
- `30_AI写作与投喂/`
- `40_源码全景底稿/`
- `50_图示与附图/`
- `80_演示与答辩/`
- `90_历史交付与模板/`

建议定位：

- `docs/` 继续作为主知识库
- 事实文档、入口文档、专题文档和历史材料继续分层维护
- 任何新增说明文档都不应再直接平铺到 `docs/` 根目录

### 4.2 `docs/30_AI写作与投喂/长庚环_AI投喂资料包/`：面向 AI 的整理层

它是从 `docs/` 主知识库中提炼出来的 AI 可读包装层，不是第一事实源。

建议定位：

- 保留
- 继续服务于 AI 投喂、答辩写作和材料提炼
- 若与事实层冲突，应先改 `docs/` 事实层，再回写这里

### 4.3 `docs/40_源码全景底稿/项目开发源码全景底稿/`：源码审阅结果包

这里保存的是系统性代码审阅底稿，适合作为重构、扩展和答辩准备时的深度参考。

建议定位：

- 保留
- 作为详版附录和审阅底稿
- 不替代 `02_当前项目现状与上下文基线.md`

### 4.4 `test-evidence/`：正式测试证据层

这个目录已经有稳定结构，不应再被当作临时缓存区。

当前主要分层为：

- `01-plan/`
- `02-unit/`
- `03-functional/`
- `04-system/`
- `cases/`
- `summary/`
- `logs/`
- `screenshots/`
- `exports/`

建议定位：

- 后续新增正式测试材料优先进入这里
- 若某份截图或日志最终要进入测试文档，应整理到对应 case 或章节下

### 4.5 `最终材料/`：当前主交付层

`最终材料/` 现在已经形成“主交付 + 辅助子目录”的稳定结构。

根目录保留：

- 正式报告 docx/pdf
- 演示视频 mp4
- 软件介绍 PDF
- 当前交付版协议文档

子目录补充：

- `安装包/`：APK
- `压缩归档/`：ZIP 分发包
- `参考快照/`：`README.pdf`
- `协议与附件/`：补充协议附件与历史归档版本

建议定位：

- 这是当前正式对外交付层
- 根目录只放“直接交付件”，补充材料统一走子目录
- 不再把截图、日志、dump 或临时中间稿塞进这里

### 4.6 `附件_评委增强理解包_完整版/`：评审解释层

这个目录当前结构已经较清晰，承担的是“面向评委和外部阅读者的二次包装”。

建议定位：

- 保留
- 继续作为对外解释包
- 内容更新应追随 `docs/`、`test-evidence/` 和 `最终材料/`

### 4.7 `材料提交版-不可以删除/` 与 `docs/90_历史交付与模板/`：历史层

这两层都应继续保留，但必须明确降级：

- 不删除
- 不作为当前工程事实第一来源
- 不作为当前正式交付第一来源

### 4.8 `artifacts/`：调试归档层

`artifacts/` 当前已明确承担“开发过程中产生的验证归档”职责。

关键子目录包括：

- `root_archive/ui_captures/`
- `root_archive/debug_logs_and_state/`
- `root_archive/crash_dumps/`
- `root_archive/db_exports/`
- `tool_output/playwright/`
- `smoke/`
- `ui-check/`
- `verification/`
- 各类专题验证目录

建议定位：

- 这是调试与验证归档层，不是正式测试交付层
- 若某项内容需要进入正式测试材料，应从这里精选后复制到 `test-evidence/`

### 4.9 `output/`：临时工具缓冲区

`output/` 当前已经清空，只保留目录说明。

建议定位：

- 允许工具临时写入
- 不作为正式材料引用
- 需要保留的稳定输出，应迁入 `artifacts/tool_output/`

### 4.10 `tools/avatar_source_assets/`：3D 源资产层

这个目录当前收口了原先散落在根目录的 3D 角色源资产：

- `5422721126842864302.vrm`
- `8590256991748008892.vrm`
- `merged-model.glb`

建议定位：

- 作为 VRM / GLB 源文件层长期保留
- 所有角色重定向、优化和导出脚本都应从这里读取源资产
- 不再把这类大文件放回根目录

## 5. 根目录当前状态与边界

第二轮整理后，仓库根目录已不再承载散落的非源码材料。

当前根目录保留的非源码文件主要属于两类：

1. 项目入口与构建配置：
   - `README.md`
   - `build.gradle.kts`
   - `settings.gradle.kts`
   - `gradle.properties`
2. 本地构建所需但不应移动的配置与密钥：
   - `local.properties`
   - `keystore.properties`
   - `changgenghuan-release.jks`

结论：

- 这些文件留在根目录是合理的
- 其他截图、日志、协议附件、数据库快照、APK、ZIP、VRM、GLB 不应再回流根目录

## 6. 当前应以哪一份材料为准

### 6.1 项目真实现状

优先级：

1. `docs/00_入口与维护/02_当前项目现状与上下文基线.md`
2. `README.md`
3. `docs/10_项目事实与架构/06_技术文档基线_2026-03-05.md`
4. `docs/40_源码全景底稿/项目开发源码全景底稿/`
5. 其他历史完成版文档

### 6.2 材料组织与去留规则

优先级：

1. `docs/00_入口与维护/03_当前材料总索引与归档建议.md`
2. `docs/README.md`
3. `docs/00_入口与维护/DOC_MAINTENANCE.md`

### 6.3 最终提交文档与交付件

优先级：

1. `最终材料/`
2. `附件_评委增强理解包_完整版/01_正式提交文档/`
3. `docs/` 中的完成版或专题说明
4. `材料提交版-不可以删除/`

### 6.4 测试证据

优先级：

1. `test-evidence/`
2. `附件_评委增强理解包_完整版/06_测试与可信度证明/`
3. `artifacts/`

### 6.5 工具输出与调试现场

优先级：

1. `artifacts/root_archive/`
2. `artifacts/tool_output/`
3. `output/`

### 6.6 3D 角色源资产

优先级：

1. `tools/avatar_source_assets/`
2. `app-shell/src/main/assets/3d_avatar/` 下的导出结果
3. `app/src/main/assets/3d_avatar/` 下的 legacy 导出结果

## 7. 当前重复关系与建议解释

当前仓库里仍存在“同一主题多层包装”的正常重复，主要包括：

- `docs/` 事实文档 -> 被提炼进 AI 投喂资料包
- `docs/` 源码全景底稿 -> 被拆分进入评委增强理解包
- `test-evidence/` 正式测试证据 -> 被提炼进评委增强理解包
- `最终材料/` 交付件 -> 被压缩为 ZIP 分发包
- 协议文档同时存在“当前交付版”和“历史归档版”

这些重复不是问题，但必须明确层级关系：

- `docs/` 与 `test-evidence/` 是内部可维护源层
- `最终材料/` 与 `附件_评委增强理解包_完整版/` 是对外打包层
- `artifacts/` 是开发过程归档层
- `tools/avatar_source_assets/` 是源资产层

## 8. 当前立即执行完毕的整理结论

这轮整理已经完成以下动作：

1. `docs/` 已按角色完成目录化整理
2. 根目录散落截图、日志、XML dump、数据库快照、崩溃日志已迁入 `artifacts/root_archive/`
3. Playwright 根级输出已迁入 `artifacts/tool_output/playwright/`
4. APK、ZIP、README PDF 和协议附件已迁入 `最终材料/` 对应子目录
5. 根目录 3D 源资产已迁入 `tools/avatar_source_assets/`
6. `test-evidence/`、`artifacts/`、`最终材料/` 等目录已补目录说明
7. 仓库根目录已恢复成“源码与配置入口”而不是“材料堆场”

## 9. 后续新增材料放置规则

### 9.1 内部事实文档

放到：

- `docs/`

### 9.2 AI 投喂和写作素材

放到：

- `docs/30_AI写作与投喂/长庚环_AI投喂资料包/`

### 9.3 正式测试证据

放到：

- `test-evidence/`

### 9.4 对外交付件

放到：

- `最终材料/`

### 9.5 评审解释材料

放到：

- `附件_评委增强理解包_完整版/`

### 9.6 调试截图、运行日志、数据库快照、崩溃现场

放到：

- `artifacts/root_archive/`
- 或具体专题验证目录

### 9.7 工具稳定输出

放到：

- `artifacts/tool_output/`

### 9.8 3D 源资产

放到：

- `tools/avatar_source_assets/`

### 9.9 不允许再发生的落位方式

不应再出现：

- 根目录截图
- 根目录日志
- 根目录 XML dump
- 根目录数据库快照
- 根目录 APK、ZIP、PDF 快照
- 根目录 VRM、GLB 源资产

## 10. 当前建议的阅读路径

如果目标是理解工程并准备重构：

1. `docs/00_入口与维护/02_当前项目现状与上下文基线.md`
2. `docs/00_入口与维护/03_当前材料总索引与归档建议.md`
3. `docs/README.md`
4. `docs/40_源码全景底稿/项目开发源码全景底稿/`

如果目标是准备正式提交或答辩：

1. `最终材料/`
2. `附件_评委增强理解包_完整版/`
3. `test-evidence/`
4. `docs/80_演示与答辩/`

如果目标是追溯开发验证现场：

1. `artifacts/root_archive/`
2. `artifacts/tool_output/`
3. `artifacts/` 中专题验证目录

如果目标是维护 3D 角色资产：

1. `tools/avatar_source_assets/`
2. `tools/build_vroid_avatar.ps1`
3. `app-shell/src/main/assets/3d_avatar/`

## 11. 后续维护清单

每次新增非源码材料后，至少检查以下事项：

- 它属于事实文档、测试证据、交付件、解释包、调试归档还是源资产
- 是否已经放进正确目录
- 是否在根目录留下了散落副本
- 是否需要同步更新 `docs/README.md` 或本文件
- 是否产生了新的 ZIP、PDF、APK、导出快照
- 是否需要把稳定工具输出从 `output/` 转存到 `artifacts/tool_output/`
- 是否需要把新的 3D 源文件转入 `tools/avatar_source_assets/`

今后若仍出现“根目录再次堆满非源码材料”，应视为违反当前维护规则，而不是重新开始新一轮整理。
