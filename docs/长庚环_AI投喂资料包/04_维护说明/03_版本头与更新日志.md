# 版本头与更新日志

- 适用任务：记录资料包版本、更新时间、覆盖范围。
- 推荐阅读优先级：中。
- 是否允许对外直接引用：内部使用为主。
- 最后核对日期：2026-03-15。

## 当前版本

- 资料包名称：长庚环 AI 全景资料包
- 版本号：`v2026.03-final-source-aligned`
- 状态：源码对齐最终版
- 维护基线日期：2026-03-15

## 本次重写覆盖范围

本次重写不是补丁，而是对 `docs/长庚环_AI投喂资料包` 的 canonical 目录整体覆盖，重点处理了以下问题：

- 清除旧资料包中的编码污染与历史漂移。
- 重新建立统一目录结构。
- 以源码为准重写 Android、Cloud、数据、时序、AI 供应链、BLE、模块职责说明。
- 把药物分析、饮食分析、桌面机器人、TTS、恢复分、Zen 交互、音景干预等近阶段变化纳入正式资料包。
- 补齐写作模板、禁写清单、统一名词表和维护规则。

## 目录版本约定

后续建议使用以下命名方式更新版本：

- `vYYYY.MM-main`
- `vYYYY.MM-source-aligned`
- `vYYYY.MM-pre-release`

不要使用含糊命名，例如：

- `最新版`
- `最终版2`
- `修正版`

## 后续更新记录模板

后续每次更新时，可按以下格式追加：

```text
### YYYY-MM-DD
- 更新人：
- 变更范围：
- 核对源码区域：
- 受影响文档：
- 备注：
```

## 2026-03-15 版本摘要

- Android 入口确认以 `app-shell` 为准。
- `app/` 明确标记为历史归档。
- `cloud-next` 后台主线确认包含 dashboard、patients、recommendations、reports、system。
- 图片分析主模型已按当前源码对齐为 `vector_engine / qwen3-vl-235b-a22b-instruct`。
- TTS 主路径已按当前源码对齐为豆包 `seed-tts-2.0-standard`。
- 桌面机器人文案生成与医生问诊结构化链已按当前 provider 顺序对齐。
