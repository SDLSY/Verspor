# 源码映射与事实来源说明

- 适用任务：说明资料包事实来源、避免引用错误、维护者交接。
- 推荐阅读优先级：中。
- 是否允许对外直接引用：内部使用为主。
- 最后核对日期：2026-03-15。

## 本资料包的事实来源

本资料包的内容以源码事实为准，优先核对以下区域：

- `app-shell`
- `core-common`
- `core-model`
- `core-data`
- `core-ble`
- `core-network`
- `core-db`
- `core-ml`
- `feature-home`
- `feature-device`
- `feature-doctor`
- `feature-relax`
- `feature-trend`
- `feature-profile`
- `cloud-next`
- `contracts`
- `ml`

## 明确不是主事实源的内容

以下内容只能作为辅助参考，不能直接作为最终事实来源：

- 历史 README
- 旧比赛文稿
- `app/` 中的历史实现说明
- 旧 AI 填充文档
- 截图和历史演示材料

## 事实优先级

建议维护时使用这套优先级：

1. 当前源码与路由、数据库、实体、ViewModel、Repository。
2. `settings.gradle.kts`、`build.gradle.kts`、`AppDatabase`、云端 API 实现。
3. `contracts` 与共享 schema。
4. 旧文档和历史材料。

## 如果源码与旧文档冲突

处理原则只有一条：

以源码为准，重写文档，不为旧文档找补。

## 建议核对的关键文件类型

- Android 入口文件。
- Room `AppDatabase` 与实体。
- ViewModel 与 Repository。
- `cloud-next/src/app/api/**`
- `cloud-next/src/lib/**`
- `contracts/README.md` 与 schema。

## 维护时最容易犯的错误

- 只看目录名字，不看真实接线。
- 把旧单体残留当成现行入口。
- 只看 UI，不看数据闭环。
- 只看云端接口，不看 Android 本地兜底。
